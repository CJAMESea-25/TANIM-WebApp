/**
 * Super admin account username (must match the `admin` table row that may create/edit other admins).
 * Kept in one place so layout nav visibility and SuperAdminPage use the same rule.
 */
export const SUPER_ADMIN_USERNAME = 'superadmin@tanim.agri';
